# APLICACAO RODANDO LOCALMENTE
# descomente caso esteja rodando fora do container docker
# spring.datasource.url= jdbc:postgresql://db_postgres:5432/postgres

# APLICACAO RODANDO NO CONTAINER DOCKER
# descomente caso esteja rodando dentro do container docker
 spring.datasource.url= jdbc:postgresql://localhost:5432/postgres

spring.datasource.username=postgres
spring.datasource.password=postgres
spring.jpa.hibernate.ddl-auto=update

spring.jpa.defer-datasource-initialization=true

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.hibernate.naming.implicit-strategy=org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# Minio Host
spring.minio.url=http://127.0.0.1:9000
# Minio Bucket name for your application
spring.minio.bucket=interacao
# Minio access key (login)
spring.minio.access-key=Y1X11MtbkxbNUUg1
# Minio secret key (password)
spring.minio.secret-key=MbP1NubtdPBrPDEDnMurhutEPGExDJl1

